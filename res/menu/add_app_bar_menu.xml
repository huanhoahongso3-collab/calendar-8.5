<?xml version="1.0" encoding="utf-8"?>
<menu
  xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@id/add_app_bar_group">
        <item android:id="@id/add_app_bar_menu_cancel" android:title="@string/discard_label" android:contentDescription="@string/discard_label" app:showAsAction="always" />
        <item android:id="@id/add_app_bar_menu_done" android:title="@string/save" android:contentDescription="@string/save" app:showAsAction="always" />
    </group>
</menu>
